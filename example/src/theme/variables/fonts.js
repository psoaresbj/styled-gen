const weights = {
    light: 300,
    regular: 400,
    semibold: 600,
    bold: 700,
};

const sizes = {
    sm: 14,
    base: 16,
    lg: 18,
    xxl: 30,
};

const fonts = {
    weights,
    sizes,
};

export default fonts